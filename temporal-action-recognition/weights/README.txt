Place action_model.onnx here OR at the Eagle repo root.

The ONNX file was not included in the zip if it was locked by another process.
Copy from your project root:

  action_model.onnx  →  weights/action_model.onnx
                   or  ../action_model.onnx (repo root)
